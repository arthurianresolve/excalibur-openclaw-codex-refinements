# Secure Coding Checklist

- Validate untrusted JSON with schemas (AJV)
- Default to **prompt** when policy is missing
- Avoid shell injection by using fixed command strings
- Log minimal, redact sensitive values
- Fail closed on invalid policy inputs
