# Clean Coding Checklist

- Small functions, single responsibility
- Explicit names (`evaluatePolicy`, `runGuardian`)
- Deterministic behavior by default
- Tests for key paths
- Docs kept adjacent to implementation
